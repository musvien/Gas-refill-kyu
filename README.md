git clone https://github.com/YOUR_USERNAME/gasfill-ku.git
cd gasfill-ku

# Create the two root folders
mkdir frontend backend

# Create placeholder files so git tracks the folders
touch frontend/.gitkeep backend/.gitkeep

# Commit the structure
git add .
git commit -m "chore: initialize gasfill-ku repo structure"
git push origin main
# Gas-refill-kyu
Gas refilling in kirinyaga university 
